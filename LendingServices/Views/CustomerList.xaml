<Page x:Class="LendingServices.Views.CustomerList"
      xmlns:conv="clr-namespace:LendingServices.Converters"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="500"
      d:DesignWidth="1200">

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#EEF2FF" Offset="0"/>
            <GradientStop Color="#F0FDFF" Offset="0.5"/>
            <GradientStop Color="#EEF2FF" Offset="1"/>
        </LinearGradientBrush>
    </Page.Background>

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:StringToVisibilityConverter x:Key="StringToVis"/>
        <conv:StringToUpperConverter x:Key="ToUpper"/>
    </Page.Resources>

    <Grid Margin="20">

        <Ellipse Width="650"
                 Height="650"
                 Fill="#3B82F6"
                 Opacity="0.08"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Top"
                 Margin="-220,-220,0,0"/>

        <Ellipse Width="500"
                 Height="500"
                 Fill="#8B5CF6"
                 Opacity="0.06"
                 HorizontalAlignment="Right"
                 VerticalAlignment="Bottom"
                 Margin="0,0,-180,-180"/>

        <Border CornerRadius="28"
                Padding="30"
                Background="#CCFFFFFF"
                BorderBrush="#40FFFFFF"
                BorderThickness="1">

            <Border.Effect>
                <DropShadowEffect BlurRadius="40"
                                  ShadowDepth="0"
                                  Opacity="0.18"/>
            </Border.Effect>

            <StackPanel>


                <Grid Margin="0,0,0,20">

                   

                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Right" 
                                VerticalAlignment="Center">

                        <TextBlock Text="{Binding TotalCustomers, StringFormat='Total: {0} customers'}"
                                   VerticalAlignment="Center"
                                   Foreground="#64748B"
                                   FontSize="16"
                                   Margin="0,0,15,0"/>

                        <Button Command="{Binding RefreshCommand}"
                                ToolTip="Refresh Data"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Padding="6"
                                Width="32" Height="32">

                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="16">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E0E7FF"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#C7D2FE"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>

                            <Path Data="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z"
                                  Fill="#3B82F6"
                                  Width="16" 
                                  Height="16"
                                  Stretch="Uniform"/>
                        </Button>
                    </StackPanel>
                </Grid>


                <Border CornerRadius="18"
                        Padding="18"
                        Background="#FFD5E9FB"
                        BorderBrush="#FF93CFF8"
                        BorderThickness="1"
                        Margin="0,0,0,18">

                    <Grid>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                        </Grid.ColumnDefinitions>

                    
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Search"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="#334155"
                                       Margin="0,0,0,6"/>

                            <Border Background="White"
                                    CornerRadius="12"
                                    BorderBrush="#E2E8F0"
                                    BorderThickness="1"
                                    Padding="8">

                                <TextBox Height="34"
                                         FontSize="18"
                                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Tag="Search by name or account no..."
                                         Foreground="Gray"
                                         BorderThickness="0"
                                         Background="Transparent"/>
                            </Border>
                        </StackPanel>


                        <StackPanel Grid.Column="1" Margin="10,0">
                            <TextBlock Text="Sort By"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="#334155"
                                       Margin="0,0,0,6"/>

                            <Border Background="White"
                                    CornerRadius="12"
                                    BorderBrush="#E2E8F0"
                                    BorderThickness="1">

                                <ComboBox Height="38"
                                          FontSize="18"
                                          SelectedIndex="{Binding SelectedSortIndex}"
                                          Background="Transparent"
                                          BorderThickness="0">
                                    <ComboBoxItem Content="Name (A-Z)"/>
                                    <ComboBoxItem Content="Name (Z-A)"/>
                                </ComboBox>
                            </Border>
                        </StackPanel>


                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Filter Status"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="#334155"
                                       Margin="0,0,0,6"/>

                            <Border Background="White"
                                    CornerRadius="12"
                                    BorderBrush="#E2E8F0"
                                    BorderThickness="1">

                                <ComboBox Height="38"
                                          FontSize="18"
                                          SelectedIndex="{Binding SelectedFilterIndex}"
                                          Background="Transparent"
                                          BorderThickness="0">
                                    <ComboBoxItem Content="All Customers"/>
                                    <ComboBoxItem Content="Active"/>
                                    <ComboBoxItem Content="Overdue"/>
                                </ComboBox>
                            </Border>
                        </StackPanel>

                    </Grid>
                </Border>

            
                <Border CornerRadius="20"
                        Padding="12"
                        Background="#FFDFF0FF"
                        BorderBrush="#FFDFF0FF"
                        BorderThickness="1">

                    <DataGrid x:Name="myDataGrid" ItemsSource="{Binding Customers}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              GridLinesVisibility="None"
                              HeadersVisibility="Column"
                              RowHeight="45"
                              Background="Transparent"
                              BorderThickness="0">

                        <DataGrid.Resources>


                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F8FAFC"/>
                                <Setter Property="Foreground" Value="#334155"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="18"/>
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="Height" Value="42"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>


                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Margin" Value="0,2"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#EEF2FF"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>


                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="10,0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                                    Padding="{TemplateBinding Padding}">

                                                <ContentPresenter VerticalAlignment="Center" 
                                                                  HorizontalAlignment="Center"/>

                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>

                        </DataGrid.Resources>


                        <DataGrid.Columns>

                            <DataGridTextColumn Header="Account No."
                                                Binding="{Binding AccountNo}"
                                                Width="*"/>

                            <DataGridTemplateColumn Header="Name" Width="2*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                                            <TextBlock Text="{Binding Name}" 
                                                       VerticalAlignment="Center" 
                                                       Foreground="#0F172A"/>

                                            <Border Padding="8,4" 
                                                    Margin="10,0,0,0"
                                                    CornerRadius="6" 
                                                    VerticalAlignment="Center">

                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Setter Property="Background" Value="#E2E8F0"/>

                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding LoanTag}" Value="Renew">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                                <Setter Property="Background" Value="#FEF08A"/>
                                                            </DataTrigger>

                                                            <DataTrigger Binding="{Binding LoanTag}" Value="Restructured">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                                <Setter Property="Background" Value="#FECDD3"/>
                                                            </DataTrigger>

                                                            <DataTrigger Binding="{Binding LoanTag}" Value="Fully Paid">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                                <Setter Property="Background" Value="#BBF7D0"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                                <TextBlock Text="{Binding LoanTag}" 
                                                           FontSize="11" 
                                                           FontWeight="Bold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#475569"/>

                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding LoanTag}" Value="Renew">
                                                                    <Setter Property="Foreground" Value="#A16207"/>
                                                                </DataTrigger>

                                                                <DataTrigger Binding="{Binding LoanTag}" Value="Restructured">
                                                                    <Setter Property="Foreground" Value="#BE123C"/>
                                                                </DataTrigger>

                                                                <DataTrigger Binding="{Binding LoanTag}" Value="Fully Paid">
                                                                    <Setter Property="Foreground" Value="#166534"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Address"
                                                Binding="{Binding Address}"
                                                Width="150"/>

                            <DataGridTextColumn Header="Contact"
                                                Binding="{Binding Contact}"
                                                Width="*"/>

                            <DataGridTextColumn Header="Loan" 
                                                Binding="{Binding LoanAmount, StringFormat='₱{0:N0}'}" 
                                                Width="*"/>

                            <DataGridTextColumn Header="Balance" 
                                                Binding="{Binding Balance, StringFormat='₱{0:N0}'}" 
                                                Width="*"/>

                            <DataGridTextColumn Header="Daily" 
                                                Binding="{Binding DailyPayment, StringFormat='₱{0:N0}'}" 
                                                Width="*"/>

                            <DataGridTextColumn Header="Due Date" 
                                                Binding="{Binding DueDate, StringFormat='MM/dd/yyyy'}" 
                                                Width="*"/>


                            <DataGridTemplateColumn Header="Status" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <Border Padding="6"
                                                CornerRadius="10"
                                                Background="{Binding StatusColor}">

                                            <TextBlock Text="{Binding Status}"
                                                       Foreground="White"
                                                       HorizontalAlignment="Center"/>
                                        </Border>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>


                            <DataGridTemplateColumn Header="Action" Width="120" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <Button Content="View Ledger"
                                                Click="ViewLedger_Click"
                                                Padding="6,3"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Cursor="Hand">

                                            <Button.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#2563EB" Offset="0"/>
                                                    <GradientStop Color="#7C3AED" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Button.Background>

                                        </Button>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>

                </Border>

            </StackPanel>
        </Border>

        <Grid Background="#40FFFFFF" 
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
              Panel.ZIndex="100">

            <Border Background="White" 
                    Padding="25,20" 
                    CornerRadius="12" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    BorderBrush="#E2E8F0"
                    BorderThickness="1">

                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Opacity="0.1" ShadowDepth="4"/>
                </Border.Effect>

                <StackPanel Orientation="Horizontal">

                    <Path Data="M 12,2 A 10,10 0 1,1 2,12" 
                          Stroke="#3B82F6" 
                          StrokeThickness="3" 
                          Width="24" Height="24" 
                          RenderTransformOrigin="0.5,0.5"
                          Margin="0,0,12,0">
                        <Path.RenderTransform>
                            <RotateTransform x:Name="SpinnerTransform" Angle="0" />
                        </Path.RenderTransform>
                        <Path.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="SpinnerTransform"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0" To="360"
                                                         Duration="0:0:0.8"
                                                         RepeatBehavior="Forever" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Path.Triggers>
                    </Path>

                    <TextBlock Text="Loading data..." 
                               VerticalAlignment="Center" 
                               Foreground="#334155" 
                               FontSize="14"
                               FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>