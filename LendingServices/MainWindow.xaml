<Window x:Class="LendingServices.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="AliceBlue" Offset="0"/>
            <GradientStop Color="#B5DCF0" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <StackPanel>
            <TextBlock FontSize="35" FontWeight="Bold" Foreground="#0C2794">
                Ang Cool Lending Services
            </TextBlock>
            <TextBlock Text="Manage customers, balances, and daily receipts" FontSize = "18" Foreground="Gray"/>
        </StackPanel>

        <Border Grid.Row="1" Panel.ZIndex="1" CornerRadius="15" Padding="10" Margin="0,20,0,20"
        Background="#66FFFFFF" BorderBrush="#40FFFFFF" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.2"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>


                <Grid.Resources>

                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="15,8"/>
                        <Setter Property="Margin" Value="10,0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Foreground" Value="#475569"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>


                        <Setter Property="MinHeight" Value="34"/>
                        <Setter Property="MinWidth" Value="90"/>

                        <Style.Triggers>


                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#2563EB"/>
                                <Setter Property="Background" Value="#EAF2FF"/>
                            </Trigger>


                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#2563EB"/>
                            </Trigger>

                        </Style.Triggers>
                    </Style>

                </Grid.Resources>

                <Button x:Name="btnDashboard" Grid.Column="0" Content="Dashboard" Click="Dashboard_Click" FontSize="18" FontWeight="SemiBold"/>
                <Button x:Name="btnCustomerList" Grid.Column="1" Content="Customer List" Click="CustomerList_Click" FontSize="18" FontWeight="SemiBold"/>
                <Button x:Name="btnAddCustomer" Grid.Column="2" Content="Add Customer" Click="AddCustomer_Click" FontSize="18" FontWeight="SemiBold"/>
                <Button x:Name="btnDailyCollection" Grid.Column="3" Content="Daily Collection" Click="DailyCollection_Click" FontSize="18" FontWeight="SemiBold"/>
                <Button x:Name="btnBorrowerCopy" Grid.Column="4" Content="Borrower Copy" Click="BorrowerCopy_Click" FontSize="18" FontWeight="SemiBold"/>
                <Button x:Name="btnLogout"
                            Grid.Column="6"
                            Content="[->Logout"
                            Click="Logout_Click"
                            FontSize="18"
                            FontWeight="SemiBold"
                            Foreground="White"
                            Background="Red"
                            BorderThickness="0"
                            Padding="18,8"
                            Margin="10,0"
                            Cursor="Hand">  

                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                        </Border>

                                        <ControlTemplate.Triggers>

                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#DC2626"/>
                                            </Trigger>

                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="Red"/>
                                            </Trigger>

                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>

                </Button>
            </Grid>
        </Border>

        <Frame x:Name="MainFrame"
               Grid.Row="2"
               NavigationUIVisibility="Hidden"
               ClipToBounds="True"/>

    </Grid>
</Window>